# Local development — copy to .streamlit/secrets.toml and fill in your key.
# For Streamlit Community Cloud, paste this content into the app's Secrets
# panel (Settings → Secrets) instead. Never commit a real key.

ANTHROPIC_API_KEY = "sk-ant-..."
